import React from 'react'

const ThemeSelector = (props) => {
    return (
        <div>
            {/* <select onChange={props.changeTheme(this.value)}>
            
                <option value={'lightTheme'}>Standard</option>
                <option value={'darkTheme'}>Dark mode</option>
            </select> */}
        </div>
    )
}

export default ThemeSelector;
